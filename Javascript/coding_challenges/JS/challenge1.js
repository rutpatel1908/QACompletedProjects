var variableString ="hello";
var variableNumber = 3;

	var variableObject = {

		name: "Elite"
	};

		console.log(variableObject.name);
        document.write(variableObject.name);
        window.alert(variableObject.name);
